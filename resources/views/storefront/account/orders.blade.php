<x-layouts.storefront title="My Orders | Cattie.uk" description="Your Cattie order history.">
<section class="shell py-12 sm:py-20"><div class="mx-auto max-w-5xl"><p class="eyebrow">My Account</p><h1 class="mt-3 font-display text-5xl">My Orders</h1>@include('storefront.account._nav')
<div class="mt-10 rounded-[2rem] bg-white p-7 sm:p-9">@forelse($orders as $order)<article class="grid gap-4 border-b border-rose/20 py-6 first:pt-0 last:border-0 last:pb-0 sm:grid-cols-[1fr_auto] sm:items-center"><div><h2 class="font-display text-2xl">{{ $order->number }}</h2><p class="mt-2 text-sm text-muted">{{ ($order->placed_at ?? $order->created_at)->format('j F Y') }} · {{ $order->status->customerLabel() }} · {{ $order->items_count }} {{ Str::plural('item', $order->items_count) }}</p></div><div class="sm:text-right"><strong class="block">£{{ number_format(($order->total_minor ?? 0) / 100, 2) }}</strong><a class="mt-2 inline-block font-bold text-coral" href="{{ route('account.orders.show', $order->number) }}">View order</a></div></article>@empty<p class="text-muted">No orders yet</p><a class="button-primary mt-6" href="{{ route('products.index') }}">Shop personalised gifts</a>@endforelse</div>
<div class="mt-8">{{ $orders->links() }}</div></div></section>
</x-layouts.storefront>
