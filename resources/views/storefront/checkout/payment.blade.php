<x-layouts.storefront :title="'Payment for '.$order->number.' | Cattie.uk'" description="Review the final total for your Cattie.uk order.">
<section class="shell py-12 sm:py-20"><div class="mx-auto max-w-3xl"><p class="eyebrow">Order {{ $order->number }}</p><h1 class="mt-3 font-display text-5xl">Complete your order</h1><p class="mt-5 leading-7 text-muted">Your artwork and final order total are ready.</p>
@if(session('payment_message'))<div class="mt-6 rounded-2xl bg-rose/20 p-4" role="alert">{{ session('payment_message') }}</div>@endif
<div class="mt-10 rounded-[2rem] bg-white p-7"><h2 class="font-display text-2xl">Order summary</h2>@foreach($order->items as $item)<div class="mt-5 grid grid-cols-[4rem_1fr_auto] items-center gap-4 border-t border-rose/20 pt-5"><img src="{{ route('artwork.assets',[$item->artworkSession->public_id,$item->generationAsset]) }}" class="h-20 w-14 rounded-lg object-cover" alt="Approved artwork"><div><strong>{{ $item->product_name }}</strong><p class="text-sm text-muted">{{ $item->variant_name }} · {{ $item->artwork_style_name }} · Qty {{ $item->quantity }}</p></div><strong>£{{ number_format($item->total_price_minor/100,2) }}</strong></div>@endforeach
<dl class="mt-6 space-y-3 border-t border-rose/20 pt-5"><div class="flex justify-between"><dt>Subtotal</dt><dd>£{{ number_format($order->subtotal_minor/100,2) }}</dd></div><div class="flex justify-between"><dt>UK delivery</dt><dd>Free</dd></div><div class="flex justify-between"><dt>Tax <span class="text-xs text-muted">(provisional)</span></dt><dd>£{{ number_format($order->tax_minor/100,2) }}</dd></div><div class="flex justify-between text-xl font-bold"><dt>Total</dt><dd>£{{ number_format($order->total_minor/100,2) }}</dd></div></dl></div>
@if($fakeEnabled)<div class="mt-7 rounded-[2rem] border-2 border-dashed border-coral/40 bg-sand p-6"><p class="eyebrow">Development payment simulator</p><p class="mt-2 text-sm text-muted">No real payment will be taken.</p><form method="POST" action="{{ route('checkout.pay',$order->number) }}" class="mt-5 flex flex-wrap gap-3">@csrf<input type="hidden" name="idempotency_key" value="{{ $paymentKey }}"><button name="scenario" value="success" class="button-primary">Complete test payment</button><button name="scenario" value="failure" class="rounded-full border border-coral px-5 py-3 font-bold">Fail</button><button name="scenario" value="cancelled" class="rounded-full px-5 py-3 font-bold text-muted underline">Cancel</button></form></div>@else<div class="mt-7 rounded-2xl bg-sand p-5">Payment is not currently available.</div>@endif
</div></section>
</x-layouts.storefront>
